[project]
name = "savings-circle"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=5.0.0",
    "fastapi[standard]>=0.141.1",
    "pyjwt>=2.13.0",
    "sqlmodel>=0.0.42",
]

[dependency-groups]
dev = []

[tool.vercel]
entrypoint = "app.main:app"
